server:
  port: 8080

spring:
  task:
    scheduling:
      pool:
        size: 1

odpt:
  api:
    base-url: https://api.odpt.org/api/v4
    consumer-key: ${ODPT_API_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
